# Ollama server URL (change if Ollama runs on a different host or port)
OLLAMA_HOST=http://localhost:11434

# Allow Ollama to process multiple VLM requests in parallel.
# Set this to the same value as --label-workers when running web_server.py.
# Requires restarting Ollama after changing.
OLLAMA_NUM_PARALLEL=1
